# Math Club Website

Math Club's Next.js app.

## Setup

1. Copy `.env.example` to `.env.local`.
2. Fill in your Supabase URL, Supabase keys, and `JWT_SECRET`.

## Run

```bash
npm install
npm run dev
```